version: '3.4'

services:
  booleanhooligans:
    image: booleanhooligans
    build:
      context: Transition Test
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 8000:8000
