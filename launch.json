{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Transition Test/package.json",
                "localRoot": "${workspaceFolder}/Transition Test"
            }
        }
    ]
}